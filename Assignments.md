1. 	Math Utility Library
• 	Build a module that exports advanced math functions (matrix multiplication, factorial, prime checker).
• 	Import it into another file and use it in a CLI calculator.

2. 	Recursive Directory Reader
• 	Write a script that recursively reads all files in a directory and subdirectories.
• 	Print a tree-like structure of the folder contents.
3. 	Log File Rotator
• 	Create a logger that writes logs to a file.
• 	When the file size exceeds 1MB, rotate it into a new file with a timestamp.

4. 	CSV to JSON Converter
• 	Read a CSV file using .
• 	Convert it into JSON format and save it as a new file.

5. 	File Watcher
• 	Use  to monitor changes in a directory.
• 	Whenever a file is added/removed/modified, log the event.
6. 	Backup Utility
• 	Write a script that copies all files from one directory to another.
• 	Add error handling for missing directories or permission issues.

7. Linked List
• 	Build a singly linked list with methods: insert, delete, find, reverse.
• 	Test it with various operations.

8. Custom setTimeout function.
• 	Implement your own version of setTimeout using Date.now() and while loops (busy waiting).
• 	Then optimize it using setInterval.


### 1. **Book Library Manager**
- Build a `Library` repository that manages books (`title`, `author`, `year`).  
- Add CRUD operations and a method to search books by author.

---

### 2. **Todo List CRUD**
- Implement a `TodoRepository` with methods to add, list, update, and delete todos.  
- Add a method to mark a todo as completed.

---

### 3. **Inventory System**
- Create an `Inventory` repository for products (`name`, `quantity`, `price`).  
- Implement CRUD plus a method to calculate total inventory value.


### 4. **Order Management**
- Build an `OrderRepository` that stores orders (`id`, `items`, `status`).  
- Implement CRUD plus a method to update order status (`pending → shipped → delivered`).

---

### 5. **Contact Manager**
- Create a repository for contacts (`name`, `phone`, `email`).  
- Implement CRUD plus a search method by name or email.

---

### 6. **Playlist Repository**
- Manage a playlist of songs (`title`, `artist`, `duration`).  
- Implement CRUD plus a method to calculate total playlist duration.

---
